<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:minHeight="64dp"
    android:paddingHorizontal="16dp"
    android:paddingVertical="12dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <TextView
        android:id="@+id/menu_text_view"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginEnd="12dp"
        android:gravity="start"
        android:textSize="16sp"
        android:textColor="@color/primary_text"
        android:lineSpacingExtra="2dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/menu_price_view"
        app:layout_constraintBottom_toBottomOf="parent"
    />
    <TextView
        android:id="@+id/menu_price_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_cafeteria_price"
        android:gravity="center"
        android:minWidth="72dp"
        android:paddingHorizontal="10dp"
        android:paddingVertical="6dp"
        android:textSize="14sp"
        android:textColor="@color/cafeteria_price_text"
        android:textFontWeight="700"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/menu_text_view"
    />
</androidx.constraintlayout.widget.ConstraintLayout>
