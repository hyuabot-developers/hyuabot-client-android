query BusStopDialogQuery($routeStops: [BusRouteStopInput!]!) {
    bus (input: $routeStops) {
        stop {
            name
            latitude
            longitude
        }
        route {
            name
            runningTime {
                up { first, last }
                down { first, last }
            }
        }
    }
}
