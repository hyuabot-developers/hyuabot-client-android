query SubwayRealtimePageQuery($weekday: String!) {
    subway (
        input: {
            keys: [
                { stationID: "K449", direction: ["up", "down"], weekdays: [$weekday] },
                { stationID: "K251", direction: ["up", "down"], weekdays: [$weekday] }
            ],
            limit: 4
        }) {
        stationID
        arrival {
            direction
            entries {
                minutes
                terminal { stationID, name }
                isRealtime
                location
                stops
                trainNumber
                isExpress
                isLast
            }
        }
    }
}
