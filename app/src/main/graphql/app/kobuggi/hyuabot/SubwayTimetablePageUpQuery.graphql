query SubwayTimetablePageUpQuery($station: String!) {
    subway (
        input: {
            keys: [
                { stationID: $station, direction: ["up"], weekdays: ["weekdays", "weekends"] },
            ]
        }) {
        timetable {
            weekday
            direction
            time
            terminal { stationID, name }
        }
    }
}
